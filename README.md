# MAPA Property — V28 FINAL14

## 🛡️ Cette version ajoute

Protection RGPD + anti-scraping sans impact SEO ni vitesse.

Voir `CHANGELOG-FINAL14.md` pour le détail.

## 🚀 Déploiement

1. Drag & drop `mapa-v28-FINAL14/` dans Netlify
2. Exécuter `sql/04-security-rls.sql` dans Supabase SQL Editor

## 🧪 Test local

```bash
cd ~/Downloads && unzip -o mapa-v28-FINAL14.zip
cd mapa-v28-FINAL14 && python3 -m http.server 8080
```

Puis `localhost:8080` + `Cmd+Shift+R`.

Console : `V28 FINAL14 — Sécurité RGPD + anti-scraping`
