// Scaffolding Sentry — INACTIF par défaut.
// Pour activer : créer un compte sentry.io, ajouter NEXT_PUBLIC_SENTRY_DSN,
// SENTRY_DSN, SENTRY_AUTH_TOKEN, SENTRY_ORG, SENTRY_PROJECT dans Vercel
// env vars puis décommenter le bloc ci-dessous.
//
// import * as Sentry from "@sentry/nextjs";
//
// Sentry.init({
//   dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
//   tracesSampleRate: 0.1,
//   replaysSessionSampleRate: 0,
//   replaysOnErrorSampleRate: 1.0,
// });

export {};
